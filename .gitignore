# Rust build artifacts
target/
**/*.rs.bk

# Cargo lock files for libraries (not binaries)
# If you have a binary crate, keep Cargo.lock committed.
# Uncomment the line below only for pure library workspaces:
# Cargo.lock

# Claude Code personal settings
.claude/settings.local.json
